<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:bind="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">


    <data>

        <variable
            name="entity"
            type="jjtelechea.example.basesatejuapp.domain.model.ChampionEntity" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="72dp">

        <ImageView
            android:id="@+id/champion_image"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_marginStart="16dp"
            android:layout_marginTop="16dp"
            android:layout_marginBottom="16dp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            bind:loadImage="@{entity.imageUrl}"
            android:contentDescription="@string/image" />

        <com.google.android.material.textview.MaterialTextView
            android:id="@+id/champion_name"
            style="@style/TextAppearance.MaterialComponents.Subtitle1"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="16dp"
            android:layout_marginEnd="16dp"
            android:maxLines="1"
            android:text="@{entity.name}"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toEndOf="@id/champion_image"
            app:layout_constraintTop_toTopOf="@id/champion_image"
            tools:text="Aatrox" />

        <com.google.android.material.textview.MaterialTextView
            android:id="@+id/champion_title"
            style="@style/TextAppearance.MaterialComponents.Caption"
            android:layout_width="0dp"
            android:layout_height="20dp"
            android:ellipsize="end"
            android:gravity="bottom"
            android:maxLines="1"
            android:text="@{entity.title}"
            app:layout_constraintEnd_toEndOf="@id/champion_name"
            app:layout_constraintStart_toStartOf="@id/champion_name"
            app:layout_constraintTop_toBottomOf="@id/champion_name"
            tools:text="The Darkin Blade" />
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>